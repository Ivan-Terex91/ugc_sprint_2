# import logging
# from config import LOGSTASH_PORT, LOGSTASH_HOST
#
# import logstash
#
# logger = logging.getLogger("Ugc FastAPI logger")
# logger.setLevel(logging.INFO)
# logger.addHandler(logstash.LogstashHandler(LOGSTASH_HOST, int(LOGSTASH_PORT), version=1))
